package FinalKeyword;

public class Notes {
	/*What is final in Java?
	 * final is a keyword and non access modifier in Java.
	 * As the name suggest if final keyword is used with 
	 * variable then that variable value becomes constant.
	 * final method can't be override.
	 * final class can't be inherited.
	 * 
	 * Whenever you declare variable in Interface, it will be by default final in nature.
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * */
}
